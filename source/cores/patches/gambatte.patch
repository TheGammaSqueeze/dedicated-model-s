diff --git a/Makefile.libretro b/Makefile.libretro
index 1842538..c6c892d 100644
--- a/Makefile.libretro
+++ b/Makefile.libretro
@@ -409,6 +409,19 @@ else ifeq ($(platform), miyoo)
    PLATFORM_DEFINES := -DDINGUX
    CFLAGS += -fomit-frame-pointer -ffast-math -march=armv5te -mtune=arm926ej-s
    CXXFLAGS += $(CFLAGS)
+   
+# TRIMUI MODEL S
+else ifeq ($(platform), trimui)
+  TARGET := $(TARGET_NAME)_libretro.so
+  CC  = $(CROSS_COMPILE)gcc
+  CXX = $(CROSS_COMPILE)g++
+  AR  = $(CROSS_COMPILE)ar
+  fpic := -fPIC
+  SHARED := -shared -Wl,-version-script=$(version_script)
+  PLATFORM_DEFINES := -DDINGUX
+  CFLAGS += -fomit-frame-pointer -ffast-math -march=armv5te -mtune=arm926ej-s
+  CXXFLAGS += $(CFLAGS)
+  # VIDEO_RGB565 = 0
 
 # Windows MSVC 2003 Xbox 1
 else ifeq ($(platform), xbox1_msvc2003)
@@ -758,3 +771,11 @@ install: $(TARGET)
 
 uninstall:
 	rm $(DESTDIR)$(core_installdir)/$(TARGET)
+
+# TRIMUI MODEL S: the generic release block above appends -O2 after the
+# platform section and the last -O flag wins, so re-override at the end
+ifeq ($(platform), trimui)
+CFLAGS   += -O3 -flto -fno-math-errno
+CXXFLAGS += -O3 -flto -fno-math-errno
+LDFLAGS  += -O3 -flto
+endif
diff --git a/libgambatte/libretro/libretro.cpp b/libgambatte/libretro/libretro.cpp
index 15b042c..fed178e 100644
--- a/libgambatte/libretro/libretro.cpp
+++ b/libgambatte/libretro/libretro.cpp
@@ -2009,7 +2009,7 @@ static enum gb_colorization_enable_type gb_colorization_enable = GB_COLORIZATION
 static std::string rom_path;
 static char internal_game_name[17];
 
-static void load_custom_palette(void)
+static bool load_custom_palette(void)
 {
    const char *system_dir = NULL;
    const char *rom_file   = NULL;
@@ -2028,7 +2028,7 @@ static void load_custom_palette(void)
    {
       gambatte_log(RETRO_LOG_WARN,
             "No system directory defined, unable to look for custom palettes.\n");
-      return;
+      return false;
    }
 
    /* Look for palette named after ROM file */
@@ -2073,7 +2073,7 @@ static void load_custom_palette(void)
    }
 
    if (!path_valid)
-      return; /* Unable to find any custom palette file */
+      return false; /* Unable to find any custom palette file */
 
    palette_file = filestream_open(palette_path,
          RETRO_VFS_FILE_ACCESS_READ,
@@ -2083,7 +2083,7 @@ static void load_custom_palette(void)
    {
       gambatte_log(RETRO_LOG_WARN,
             "Failed to open custom palette: %s\n", palette_path);
-      return;
+      return false;
    }
 
    gambatte_log(RETRO_LOG_INFO, "Using custom palette: %s\n", palette_path);
@@ -2188,6 +2188,7 @@ palette_line_end:
    }
 
    filestream_close(palette_file);
+   return true;
 }
 
 static void find_internal_palette(const unsigned short **palette, bool *is_gbc)
@@ -2534,6 +2535,9 @@ static void check_variables(bool startup)
 
    switch (gb_colorization_enable)
    {
+      case GB_COLORIZATION_CUSTOM:
+          if (load_custom_palette()) break;
+		  gb_colorization_enable = GB_COLORIZATION_AUTO;
       case GB_COLORIZATION_AUTO:
          // Automatic colourisation
          // Order of preference:
@@ -2579,9 +2583,6 @@ static void check_variables(bool startup)
          if (!gbc_bios_palette)
             find_internal_palette(&gbc_bios_palette, &isGbcPalette);
          break;
-      case GB_COLORIZATION_CUSTOM:
-         load_custom_palette();
-         break;
       case GB_COLORIZATION_INTERNAL:
          find_internal_palette(&gbc_bios_palette, &isGbcPalette);
          break;
